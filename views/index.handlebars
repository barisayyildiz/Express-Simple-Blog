
<div class="list-group">

	{{#each blogs}}

		{{#if @first}}

			<a class="list-group-item list-group-item-action flex-column align-items-start active" >
				<div class="d-flex w-100 justify-content-between">
					<h4 class="mb-1 title">{{this.title}}</h4>
					<small>{{this.date}}</small>
				</div>
				<p class="mb-1 text">{{this.text}}</p>
				<!--<button type="button" class="btn btn-danger" id="dltBtn" myattr="{{this_id}}" onclick="deleteItem(this)">Delete</button>-->
				<form method="POST" action="/delete/{{this._id}}?_method=DELETE">
					<input type="hidden" name="_method" value="DELETE">
					<button type="submit" class="btn btn-danger">Delete</button>
				</form>
			</a>

		{{else}}

			<a class="list-group-item list-group-item-action flex-column align-items-start">
				<div class="d-flex w-100 justify-content-between">
					<h4 class="mb-1 title">{{this.title}}</h4>
					<small>{{this.date}}</small>
				</div>
				<p class="mb-1 text">{{this.text}}</p>
				<!--<button type="button" class="btn btn-danger" id="dltBtn" myattr="{{this_id}}" onclick="deleteItem(this)">Delete</button>-->
				<form method="POST" action="/delete/{{this._id}}?_method=DELETE">
					<input type="hidden" name="_method" value="DELETE">
					<button type="submit" class="btn btn-danger">Delete</button>
				</form>

			</a>
		{{/if}}

	{{/each}}
	
</div>
